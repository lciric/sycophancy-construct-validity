"""Ridge probe extraction (placeholder; the code is not versioned here).
Ridge regression on judge-scored behaviour gave a candidate sycophancy direction in the residual stream of
Llama 3.1 8B Instruct; the probe's own fit metrics were training fit and are not reported.
The extraction ran in a Colab notebook that is not in this repository; the only file in results/ that uses
its direction is sae_residual_analysis.json (proj_probe), which carries the same training-fit reservation."""
